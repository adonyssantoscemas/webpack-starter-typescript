import db from './firestore/config'

const user = {
	name: "Evelyn",
	active: true,
	birthday: 0
}

// db.collection('users')
// 	.add( user )
// 	.then( ref => {
// 		console.log(ref.delete());
// 	} )
// 	.catch( e => console.log("error", e))

